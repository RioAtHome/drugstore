import { ExtensionValidatorDirective } from './extension-validator.directive';

describe('ExtensionValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtensionValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
