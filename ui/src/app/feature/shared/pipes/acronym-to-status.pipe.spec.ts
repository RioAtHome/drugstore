import { AcronymToStatusPipe } from './acronym-to-status.pipe';

describe('AcronymToStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new AcronymToStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
